# Hardware revisions

Create one directory per immutable revision, for example `A0-development` or
`B1-alpha`. Each revision must identify:

- schematic/PCB/CAD source versions;
- exported manufacturing files and checksums;
- BOM revision;
- firmware compatibility;
- assembly instructions;
- known deviations;
- validation results.

Never overwrite manufacturing outputs for a released revision.
